package Day${DAY};

import Base.AdventBase;
import java.util.List;

public class Part${PART} extends AdventBase {
    public static int Run(boolean example) {
        Start(${DAY}, ${PART}, example);

        List<String> input = LoadInput(${DAY}, example);

        return 0;
    }
}